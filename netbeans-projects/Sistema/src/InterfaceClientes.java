// listener da classe RelDef

public interface InterfaceClientes {

    // invocado pela classe RelDef para informar que o campo de texto foi alterado
    public void campoTextoAlterado(String novoTexto);
}
